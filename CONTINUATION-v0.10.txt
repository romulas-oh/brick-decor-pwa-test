PROJECT CONTINUATION — BRICK & DECOR PWA
CURRENT SOURCE BASE: v0.10 L1.4 Workflow Patch

Latest packaged build:
brick-decor-pwa-v0.10-l1.4-workflow.zip

Build lineage:
v0.10 -> v0.9 -> v0.8 -> v0.7 Case-Centric -> v0.6.2 Full Integrated -> v0.5.1 Premium

DO NOT rebuild from an older/smaller demo.

CURRENT CONFIRMED RULES ADDED IN v0.10

1. Manual/offline Client Approval requires uploaded customer-signed evidence before confirmation.
2. Customer E-sign remains an additional method; existing/manual method must remain.
3. Frontend E-sign link UI exists, but real cross-device secure submission/sync requires backend.
4. Supplier invoice is line-item based with:
   - supplier
   - invoice no.
   - invoice date
   - Case/Project
   - GST yes/no + rate
   - due date
   - attachment
   - item
   - description
   - unit cost
   - quantity
   - approved quotation/VO item match
5. A Case may have multiple accepted quotations. Supplier match selector combines approved items rather than forcing a single quotation first.
6. Multiple supplier invoices can match the same quotation/VO item.
7. Approved matched supplier invoice lines post into Actual Project Cost once only.
8. Project Cost reconciliation rows show:
   - Location
   - Item
   - Estimated Cost (Quotation)
   - Charge Amount
   - Actual Cost from Supplier
   - Cost Variance
   - Actual Margin
9. Supplier Invoice Aging filters by date/supplier/project/company and exports Excel-compatible .xls.
10. Latest/newest operational records should display at the top.
11. Duplicate supplier invoice rule remains supplier + invoice number only.
12. v0.9 template bug fixed:
    - unlocked/draft documents follow latest Company Master wording
    - explicit Issue/Lock stores snapshot
    - issued copy remains unchanged afterward
    - sent quotation revisions remain immutable

FRONTEND LIMITATION / FUTURE BACKEND

For production E-sign implement secure server-side signing links:
- cryptographically secure random token
- expiry/single-use/revocation
- immutable quote revision payload/PDF
- customer signer name/signature/time
- audit record
- private evidence file storage
- return signed approval into staff Case File

CURRENT TEST STATUS

- JS syntax checks PASS
- service worker asset integrity PASS
- v0.10 runtime regression 16/16 PASS
- all 10 Case tabs repeatedly rendered in harness
- GitHub NOT changed
- live site NOT published

NEXT SAFE STEP

Upload/extract v0.10 into existing GitHub test repository, allow Pages to deploy, then do a short visual smoke test on:
- Case -> Quotation -> Generate Customer E-sign Link
- Manual Client Approval evidence requirement
- PO/Supplier Aging -> Add Supplier Invoice
- GST totals + multiple line items
- approved item matching
- Supplier Invoice filters / Export Excel
- Assigned ID approval
- Case -> Project Cost reconciliation
- Company Master template edit -> unlocked invoice output change
- Issue/Lock -> later Company Master edit does not alter issued copy
- mobile layout

Do not ask the user to repeat v0.8/v0.9 functionality unless v0.10 directly touched it.
